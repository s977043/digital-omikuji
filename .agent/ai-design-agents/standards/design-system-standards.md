# Design System Standards

## Purpose

コンポーネントとトークンの再利用性を守るための基準です。

## Principles

- まず既存部品で置き換え可能かを確認する
- 色名より意味名で設計する
- variant は必要最小限に抑える
- API 命名は体系で揃える

## Required Checks

- token を経由せず値を直書きしていないか
- 同等のコンポーネントが重複していないか
- バリアントが用途に対して過剰でないか
- 破壊的変更の移行方針があるか
