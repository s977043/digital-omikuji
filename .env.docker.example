# Docker environment variables for Expo development
# Host IP address for Metro bundler
# Run `hostname -I` (Linux/WSL) or `ipconfig` (Windows) to find your local IP
HOST_IP_ADDRESS=your_local_ip_here
