# ローカル環境でのデプロイバージョン表示機能の確認方法

## 環境準備

### 1. 依存関係のインストール
```bash
pnpm install
```

## 開発サーバーの起動

### 2. Expo 開発サーバーの起動
```bash
# Web 開発サーバーを起動
pnpm web

# または（従来の方法）
pnpm start
```

### 3. サーバー起動後のメッセージ
以下のいずれかのメッセージが表示されます：
```
To run the app with live updates:
  › Metro waiting on exp://xxx.xxx.xxx.xxx:19000
  › Scan the QR code above with Expo Go to open your app

Web interface available at:
  › http://localhost:8081
  › http://localhost:8081/?embedded=true
```

## 機能確認手順

### ✅ **確認 1: コンソール出力の確認**

#### Web 開発サーバーで確認
```bash
# ブラウザで以下 URL を開く
http://localhost:8081

# ブラウザの開発者ツール（F12）を開く
F12 キーを押す → Console タブをクリック

# コンソールに以下のようなメッセージが表示される：
# 🚀 Digital Omikuji v0.1.1 | 📅 Build: 2026/1/1 22:12:00 | 🌍 Env: development
```

期待される出力：
- ✅ 緑色の太字で表示
- ✅ バージョン番号：v0.1.1
- ✅ ビルド日時：ISO 形式の日時
- ✅ 環境：development （ローカル環境のため）

### ✅ **確認 2: UI 表示の確認**

Web ブラウザで以下を確認：

1. **メイン画面 (index.tsx)**
   - URL: http://localhost:8081
   - 右下角に薄いグレー色で「v0.1.1」が表示されること
   - 表示スタイル：text-xs, text-white/40, font-mono（モノスペース）

2. **履歴画面 (history.tsx)**
   - 「運勢手帳」ボタンをクリック
   - 右下角に同じように「v0.1.1」が表示されること

表示確認ポイント：
- ✅ 位置：画面の右下角
- ✅ フォント：モノスペース（等幅フォント）
- ✅ 色：白（40% 透明度）
- ✅ テキスト：v0.1.1

### ✅ **確認 3: テストの実行**

```bash
# VersionInfo のテストを実行
pnpm test -- VersionInfo.test.ts

# 期待される結果
PASS utils/__tests__/VersionInfo.test.ts
  VersionInfo
    getVersionInfo
      ✓ should return version info object with required properties
      ✓ should return a valid package version
      ✓ should return a valid ISO date string for buildTime in development
      ✓ should have a valid environment
    getVersionDisplay
      ✓ should return a short version string
    getDetailedVersionInfo
      ✓ should return a detailed version string

Test Suites: 1 passed, 1 total
Tests:       6 passed, 6 total
```

## ファイル確認

実装されたファイルの確認：

### 新規作成ファイル
```bash
ls -la components/VersionDisplay.tsx
```
期待される結果: ファイルが存在する (14 行)

### 修正ファイル
```bash
# バージョン情報取得ユーティリティ
head -20 utils/VersionInfo.ts

# 表示コンポーネント使用確認
grep "VersionDisplay" app/index.tsx app/history.tsx
```

## トラブルシューティング

### サーバーが起動しない場合
```bash
# キャッシュをクリア
rm -rf .expo
rm -rf dist
pnpm install
pnpm web
```

### コンソール出力が表示されない場合
1. ブラウザをリロード（F5 キー）
2. 開発者ツールのコンソールを確認（F12）
3. フィルターが「Errors」になっていないか確認

### バージョンが「unknown」と表示される場合
```bash
# app.json のバージョンを確認
grep -A 2 '"version"' app.json

# 期待される出力：
# "version": "0.1.1",
```

## 環境情報

確認済みの環境：
- Node.js: v22.20.0
- pnpm: 9.15.2
- Expo SDK: 52
- React Native: 最新

## 次のステップ

1. ✅ ローカルでの動作確認完了
2. ⏳ Vercel への自動デプロイ確認
3. 🚀 本番環境（https://digital-omikuji.vercel.app）での動作確認

